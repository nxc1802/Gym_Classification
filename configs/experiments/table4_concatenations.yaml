# Experiment Table 4: Concatenated Feature Methods on Relative 12x4+1 Landmarks and Angle Features
# Concatenation modes: Branch-Concat vs Direct-Concat
# Models: LSTM, Bi-LSTM, Transformer

models:
  - "LSTM"
  - "BiLSTM"
  - "Transformer"

methods:
  - "branch_concat"
  - "direct_concat"

hyperparameters:
  batch_size: 16
  epochs: 100
  patience: 20
  learning_rate: 0.0001
